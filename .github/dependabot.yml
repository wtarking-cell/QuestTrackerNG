version: 2
updates:
  # Keep GitHub Actions current (catches Node-runtime deprecations early).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    labels:
      - ci
